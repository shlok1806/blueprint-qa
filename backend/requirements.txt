fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
# greenlet is required by SQLAlchemy's async engine. Plain `sqlalchemy` does not
# install it - only the [asyncio] extra does. Without it every DB-backed route
# raises "the greenlet library is required to use this function" as a 500, while
# /health still returns 200. Pinned explicitly so it can never be dropped again.
greenlet==3.5.5
psycopg[binary]==3.2.3
alembic==1.14.0
pydantic==2.10.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
python-multipart==0.0.17
httpx==0.28.0
openai==3.1.0
pdf2image==1.17.0
pytesseract==0.3.13
Pillow==11.0.0
aiofiles==24.1.0
# Optional: only required when STORAGE_BACKEND=azure
azure-storage-blob==12.23.1
